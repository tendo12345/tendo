# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env files. `.env.local` is already covered by *.local, but the bare forms are not, and
# this repo now has env config worth not committing.
.env
.env.*
!.env.example

# Baseline written by scripts/strip-data-columns.ts for verify-data-parity.ts
.data-fingerprints.json

.vercel

# scripts/build-brand.py and capture-ground-truth.py are run directly, never imported, but
# Python still writes bytecode next to them the moment anything does import one.
__pycache__/
